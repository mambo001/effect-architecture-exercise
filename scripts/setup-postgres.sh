# TODO: Initialize db by adding the following tables: users, todos
# postgres requires ~/.pgpass file to authenticate by password
# .pgpass should contain the lines of the following format:
# hostname:port:database:username:password